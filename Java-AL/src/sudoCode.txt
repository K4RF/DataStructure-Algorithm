D[i][j] (i, j 위치에서 왼쪽 위로 만들 수 있는 최대 정사각형 길이를 저장하는 배열)
n(배열의 세로 길이)
m(배열의 가로 길이)
max(최댓값 저장하기)
for(i -> 0 ~ n){
    for(j -> 0 ~ m){
        D[i][j]의 값이 1이면 자신의 위, 왼쪽, 대각선 위의 값들 중 최솟값 + 1값 저장하기
        D[i][j]의 값이 최댓값보다 크다면 최댓값 업데이트
    }
}
정답(최댓값 * 최댓값) 출력하기