Min(최솟값) Max(최댓값)
Check(Min ~ Max 사이 반복, i * i 증가){
    pow(제곱수)
    start_index(최솟값/제곱수)
    for(j = start_index ~ Max 사이 반복하기){
        j * pow가 Max보다 작을 때 최솟값, 최댓값 사이의 제곱수이므로
        Check 배열에 저장하기
    }
}
count(제곱이 아닌 수 카운트)
for(0 ~ Max-Min){
    Check 배열에서 제곱이 아닌 수라면 count 증가
}
count 출력하기