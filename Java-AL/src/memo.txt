N(어떤 수)
A(소수 배열)
for(2 ~ 10000001){
    A 배열 초기화하기
}

for(A 배열 길이의 제곱근까지 반복하기){
    소수가 아니면 넘어감
    for(소수의 배수 값을 10000001까지 탐색하기){
        이 수가 소수가 아니라는 것을 표시하기
    }
}
while(true){
    N부터 값을 1씩 증가시키면서 해당 갓ㅂ이 소수이면서 팰린드롬 수인지 판별하기
    맞으면 반복문 종료하기
}

Integer 값을 char 배열로 변환하기
s(시작 인덱스), e(끝 인덱스)
while(s < e)
만약 시작과 끝 인덱스에 해당하는 값이 다르면 return false;
s++; e--;
반복문을 다 돌았으면 return true;