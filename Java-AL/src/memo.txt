n(소인수 표현) result(결괏값)
for(2 ~ n의 제곱근){
    if(현재 값이 소인수라면){
        결괏값 = 결괏값 - 결괏값 / 현재값
        n에서 현재 소인수 내역을 제거하기(2^7 * 11 * 13 -> 현재 소인수가 2일 때 11 * 13으로 변경)
    }
}
if(n > 1){
    결괏값 = 결괏값 - 결괏값 / n
}
결괏값 출력하기